# profiles.yml dành riêng cho Airflow container.
# Được mount vào /home/airflow/.dbt/ trong docker-compose.
# Đọc credentials từ env vars (cung cấp qua env_file: ../.env).

epl_athena:
  target: dev
  outputs:
    dev:
      type: athena
      region_name: "{{ env_var('AWS_REGION', 'ap-southeast-1') }}"
      s3_staging_dir: "s3://epl-pipeline-processed-nqh/athena-results/"
      s3_data_dir: "s3://epl-pipeline-processed-nqh/dbt/"
      schema: epl_gold
      database: awsdatacatalog
      aws_access_key_id: "{{ env_var('AWS_ACCESS_KEY_ID') }}"
      aws_secret_access_key: "{{ env_var('AWS_SECRET_ACCESS_KEY') }}"
      work_group: primary
      threads: 4
